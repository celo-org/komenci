{
  "name": "@komenci/funder",
  "version": "1.0.0",
  "description": "Komenci service that distributes funds to relayers",
  "main": "dist/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "npx nest build",
    "clean": "rm -rf ./dist",
    "start:dev": "npx nest start --watch",
    "start:prod": "node dist/main.js",
    "test": "jest --runInBand",
    "test:watch": "jest --runInBand --watch",
    "test:cov": "jest --coverage --runInBand",
    "test:debug": "node --inspect-brk -r tsconfig-paths/register -r ts-node/register node_modules/.bin/jest --runInBand",
    "test:e2e": "jest --config jest.config.e2e.js --runInBand",
    "test:e2e:debug": "node --inspect-brk -r tsconfig-paths/register -r ts-node/register node_modules/.bin/jest --config jest.config.e2e.js --runInBand"
  },
  "author": "Bogdan Dumitru <bogdan@clabs.co>",
  "license": "Apache-2.0",
  "devDependencies": {
    "@nestjs/cli": "^7.6.0",
    "fetch-mock": "^9.11.0",
    "ts-jest": "^26.5.5",
    "source-map-support": "^0.5.19",
    "jest-fetch-mock": "^3.0.3"
  },
  "dependencies": {
    "@komenci/blockchain": "^1.0.0",
    "@komenci/core": "^1.0.0",
    "@komenci/logger": "^1.0.0"
  }
}
